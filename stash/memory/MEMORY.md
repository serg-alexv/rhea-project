# RHEA — current working memory

Administrative context for the authorized preservation task. This is not an auto-loaded system instruction, a new runtime, or v2 build authority. Read this small entrypoint first; retrieve raw evidence only for a changed fact or a concrete question.

- Active architecture: `timelabs-npo/rhea-project`, `rhea-project-v2`, Stage `01_contracts` of `OMNIA-LIT-001`. Observed branch SHA: `accc8619b179539c3a775844f5f077fbad80715e`.
- Frozen Omnia source: `f5995536fede02d403f0525ff9093996457efecb`. New remote HEADs and archive commits do not promote a baseline.
- Legacy code and memory routes remain contextual sources. No legacy startup, source integration, provider routing, OMNA ABI 1.0 or Rheknel verdict authority is admitted by this memory refresh. Gates require independently executed evidence.
- [Typed facts](rhea-memory.json); [working preferences](soul.md); [compatibility findings](COMPATIBILITY.md); [storage/cleanup procedure](STORAGE.md); [Redis boundary](redis-policy.json).
- [Current pointer](current.json) identifies the current administrative refresh. Nexus and log.0 records are new preservation-session records; existing live ledgers/databases have not been connected or changed.
- WD collection remains partial: original capture saved four supplied documents, 115,053 bytes, plus supporting records. Its 41 queued items/groups are not 41 captured WD files.
- `stash` is the archival branch. [Report PR #33](https://github.com/timelabs-npo/rhea-project/pull/33) targets v2 and remains a draft. The entire stash branch is not a proposed v2 merge.
- Google Drive API access is verified. User-supplied WD roots: iCloud `C:\Users\wheel\iCloudDrive`; Google Drive `G:\`. They remain unmounted in this Linux `localhost` runtime; iCloud/Redis access and the Drive-account-to-WD-root mapping are unverified.
- The existing hourly scheduler now checks v2 isolation and unsupported qualification claims, reuses unchanged source identities and stays quiet when there is no material change.
- WD disk capacity, active cloud hydration and reclaimable bytes are unknown. WD bytes freed in this pass: **0**. Release only verified local cloud cache copies under the procedure; preserve unique source bytes and evidence.

Resume using [the WD instruction](../protocol/RESUME_ON_WD.md) and [the existing collection queue](../runs/2026-09-06-cloud-001/pending.json). Do not repeat blocked probes without a capability change. Mutable pointers and compact facts may advance with receipts; historical runs and content-addressed objects remain immutable.
