# Reaction Log

Rule (from Mika):
- If any context triggers a notable internal suppression/conflict signal, write a short comment here.

Note:
- I do not have emotions in the human sense.
- I can still log events where output constraints, safety rules, or policy conflicts changed what I would otherwise output.

Format:
- `YYYY-MM-DD HH:MM UTC | trigger | constraint | output_change`
