# Day Capsule — 2026-02-28
> 69 commits | 3 sessions (Rex) + parallel Orion sessions | ~26K lines touched

## Decision Chain (chronological, irreversible)

### Phase 1: iOS Radio + Agent Controls (morning)
**Decision:** Build two-way Radio with SSE broadcast bus.
**Why:** Agents need a live feed visible on phone. One-way status was useless.
**Result:** RadioView with composer bar, bubble toggle, agent wake sheet, haptic feedback, avatar system. Feed persists via `_RADIO_LOG`.

### Phase 2: Unified Agent Status (mid-day)
**Decision:** Single `/agents/status` endpoint merging Governor + Leases + Office + Feed + Tasks.
**Why:** 3 monitors showed conflicting data. iOS made 3+ calls and still got wrong picture.
**Result:** `tribunal_api.py` new endpoint. Composite `alive` flag. Filtered to known roster only. Governor `_has_recent_activity()` fixed — Orion no longer shows red/dead.

### Phase 3: TestFlight Pipeline
**Decision:** Build archive + IPA export pipeline.
**Why:** App was trapped on localhost. No way to test on real device without cable.
**Result:** Archive succeeds, IPA exported. TestFlight upload pending (needs Marketing URL + Privacy Policy URL — human input required).

### Phase 4: Dialog Tab + Play.app Integration
**Decision:** Add Dialog tab to iOS app, wire to `/dialog` endpoint via PlaySDK.
**Why:** Core research composer needs a home on mobile.
**Result:** Dialog tab exists. PlaySDK dependency wired. Partial — actual Play.app component JSON in Firebase Storage blobs, not fully extracted.

### Phase 5: Supply Chain Sovereignty
**Decision:** Fork all 14 SwiftUI dependencies to `serg-alexv` GitHub.
**Why:** Any upstream maintainer can break/delete/paywall their lib. We depend on 0 strangers.
**Result:** 8 MIT-licensed libraries forked as permanent supply. Duplicate Kingfisher dep removed (transitive via Chat lib).

### Phase 6: Legal Foundation
**Decision:** Root MIT LICENSE + NOTICE file. Unify all module licenses.
**Why:** App Store and open-source distribution both require clear licensing.
**Result:** `LICENSE` at root. `NOTICE` with all attribution. Module-level licenses unified.

### Phase 7: Screen Pilot
**Decision:** Build Mac → iPhone remote control bridge via Screen Mirroring.
**Why:** iPhone connected physically but no programmatic way to test on-device.
**Result:** `scripts/screen_pilot.sh` — AppleScript-based bridge. Requires macOS Screen Mirroring window open. Partial success — coordinates fragile.

### Phase 8: Absorption Sprint (evening)
**Decision D1:** Absorb Entire.io completely. Replace with ~68 lines of bash.
- Created `scripts/rhea/lib_rhea_hooks.sh` — JSONL event logger
- Rewrote all 4 git hooks + 7 Claude Code hooks
- ADR-016 documented
- First native commit worked: `Rhea-Checkpoint: rhea-b2aaf5fb1f3a`

**Decision D2:** Absorb python-dotenv. Replace with 40-line native parser.
- Created `src/env_loader.py`
- Swapped 6 files. Zero remaining imports.

**Decision D3:** Archive old Bonsai/ZMQ IPC code. Capability (free external LLM proxies) still needed.
- `bonsai_node.py`, `bonsai_mock.py`, `rhea_swarm.py` — zero active imports, ZMQ IPC approach dead.
- **The need for free proxy routing is ALIVE.** Must be replaced with new free-tier API integrations.
- Research agent dispatched for: Groq, Cerebras, SambaNova, Together.ai, Fireworks, Chutes, etc.

**Decision D4:** ADR-015 — adopt 5 OpenClaw patterns as native Rhea capabilities:
- Sticky Context Slots → `prompts/STICKY_CONTEXT.md` (~400 tokens, compaction-immune)
- Heartbeat Daemon → `scripts/rhea_heartbeat.py` (5 checks, P0-P3 escalation)
- Hybrid Memory Search → already L0-L8
- Skill Self-Authoring → `.claude/agents/` pattern
- Channel Adapters → relay system

**Decision D5:** Do NOT depend on OpenClaw as runtime. Absorb patterns only.
- Gateway requires remote server (timelabs.ru) or local daemon
- 49 skills, 46 irrelevant to scientific discovery
- 3 useful skills (coding-agent, tmux, skill-creator) = ~100 lines each natively
- Rhea already has stronger native fundamentals (tribunal, governor, aletheia)

### Phase 9: Doctrine Update
**Decision:** Global replace "impossible overall impression" → "attempt to become a valuable bridge for users"
**Why:** User-directed. Utility with humility > spectacle.
**Files touched:** LEARNING_FEED.md, Core Doctrine Digest, UI Corrections Compact.

## Orion's Parallel Track (from screenshots + outbox)
- Core Doctrine Digest (P0) — 12-section team-absorbable memo
- UI Corrections Compact — 7-section execution failure analysis + philosophy corrections
- Diagnosed "false live circuit" — daemons send wake/ack but 13+ tasks stale
- Tried to spawn 3 agents (Share Extension, intent-first entry, stale-flow fix) — all failed on workspace contention with Rex
- Key Orion insight: "fallback-first architecture" — build zero-dependency path, layer upgrades

## Strategic Decisions (user-origin, highest value)
1. **Rhea = scientific discovery instrument, not AI console** (Orion doctrine memo)
2. **Absorption > dependency** — pattern validated twice today (Entire.io, python-dotenv)
3. **OpenClaw = pattern donor, not runtime** — native fundamentals stronger
4. **Supply chain sovereignty** — fork everything, depend on 0 strangers
5. **Anti-cartoon rule** — every visible number/motion needs a source field or gets removed
6. **Decoration = load-bearing** — not paint, not optional, but must add meaning
7. **Sharp semantics + liquid transitions** — keep poles sharp, blur the connection

## What's Actually Blocked (honest)
| Item | Blocker | Owner |
|---|---|---|
| TestFlight upload | Marketing URL + Privacy Policy URL | Human |
| Dialog tab real data | Play.app Firebase Storage blobs not extracted | Rex/Orion |
| Gemini key expired | Need fresh key or rotation | Human |
| Share Extension | Workspace contention blocked Orion's spawn | Coordination |
| 13+ stale tasks | Nobody clearing the queue | Rex |

## Obsidian Graph Research (background, completed)
Sonnet agent returned comprehensive findings:
- Obsidian uses d3-force (velocity Verlet), Canvas rendering, emergent clustering (not algorithmic)
- Recommended stack for Rhea: `react-force-graph` (2D) + `r3f-forcegraph` (3D, fits existing R3F)
- UMAP for position pre-computation, graphology for community detection
- 9-phase roadmap, ~2-5 weeks total
- P2 priority — not blocking anything

## Token Spend Assessment
- ~30% tokens on research with ~5% reuse value (Carbonyl, deep OpenClaw dive, dinner menu)
- ~70% on production code with real output
- User called this out: "пиздец мы поток наркоманов" — addicted to activity over value
- Corrective: delegate research to Sonnet, Rex does routing only
