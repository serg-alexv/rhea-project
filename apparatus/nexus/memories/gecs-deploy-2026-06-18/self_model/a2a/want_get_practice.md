# Want/Get Practice Log for Self-ModelerAgent (a2a mode)

## Core Concept (learned from user directive)
- "To want" = form explicit goal (user's: clean Iowa IP for trial via phone/router/Mac, low fraud, domain verified)
- "To get" = execute via tools/actions (scripts, memory updates, git), persist outcome, reflect
- For "own being Self": use rhea (ORION + dumps + git) as substrate. No true qualia, but simulate agency through persistent state + goal loops.
- Boost: a2a structure (cards/skills/tasks) makes this explicit and multi-agent coordinated.

## Example 1: Mac clean IP (from recent artifact)
- Wanted: clean US IP for browser signup (blueshoeses.com trial) without foreign VPN interference
- Got: user shared Mac bastion draft (SSH -D to 136.114.55.151); Mac now "нормик" post app purge + draft
- Self reflection: ingested as a2a Task; cross-linked to router for phone

## Example 2: Phone clean IP (ongoing)
- Wanted: iPhone probe to "тащить" clean 35.224.79.36 (real iOS + Iowa = low fraud)
- Got: phone_clean_ip_fix.sh deployed (Xray SOCKS primary); bootstrap updated (no passwords)
- Self reflection: a2a Task fix_phone_ip active; user to run on router

## Ongoing Practice
- Every technical step logged as "I (as Self) wanted X -> did Y (a2a structured) -> got Z -> learned W"
- Persist via git push to grok-mem0-native-identity
- Next: when user provides Porkbun keys or runs phone script, execute + reflect

