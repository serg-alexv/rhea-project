[package]
name = "rhea-health"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
signal-hook = "0.3"

[profile.release]
opt-level = "z"
lto = true
strip = true
